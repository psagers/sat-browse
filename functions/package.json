{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "serve": "yarn exec -s firebase emulators:start",
    "shell": "yarn exec -s firebase functions:shell",
    "deploy": "yarn exec -s firebase deploy",
    "logs": "yarn exec -s firebase functions:log"
  },
  "engines": {
    "node": "12"
  },
  "main": "index.js",
  "dependencies": {
    "@tinyhttp/content-disposition": "^0.5.1",
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.1",
    "got": "^11.7.0",
    "html-to-text": "^5.1.1",
    "nodemailer": "^6.4.14"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.2.0",
    "firebase-tools": "^8.13.1",
    "shadow-cljs": "^2.11.5"
  },
  "private": true
}
